apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-ca.fullname" . }}
  labels:
    {{- include "cluster-ca.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-ca.selectorLabels" . | nindent 4 }}
