apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-ca.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-ca.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-ca.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
